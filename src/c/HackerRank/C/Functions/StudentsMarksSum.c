#include <stdio.h>

int main()
{
    int studentAmount;
    scanf("%d",&studentAmount);
    int studentGrade[studentAmount];
    for (int i = 0; i < studentAmount; i++)
    {
        /* code */
    }
    
    return 0;
}